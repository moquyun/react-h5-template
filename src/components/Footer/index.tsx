import React, {
  useState,
  useEffect,
  // useRef,
  // useCallback,
  // useImperativeHandle,
  // forwardRef,
} from 'react'
import style from './index.less'

interface PropsType {
}

const Footer = (props: PropsType, ref) => {

  return (
    <div className={style['main']}>
      Component
    </div>
  )
}

export default Footer
